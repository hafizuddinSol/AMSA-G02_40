import 'package:flutter/material.dart';

class DisplayFeedingTimePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Display Feeding Time Page'),
      ),
      body: Center(
        child: Text('This is the Display Feeding Time Page'),
      ),
    );
  }
}
