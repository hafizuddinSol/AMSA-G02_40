import 'package:flutter/material.dart';

class pHLevelPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('pH Level Page'),
      ),
      body: Center(
        child: Text('This is the pH Level Page'),
      ),
    );
  }
}
